---
summary: "Scope npm package to @cyanheads/nist-nvd-mcp-server."
breaking: false
---

# 0.1.2 — 2026-05-24

## Fixed

- **Package name** scoped to `@cyanheads/nist-nvd-mcp-server` in `package.json`, `server.json` (`packages[].identifier`), and all README badges (npm badge URL, Cursor base64 config, VS Code URL-encoded config)
- **`bundle` script** updated to use `bun run build` instead of `npm run build`
- **`author` field** aligned to the `cyanheads <email> (url)` format used across the ecosystem
