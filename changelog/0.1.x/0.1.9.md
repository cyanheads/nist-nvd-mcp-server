---
summary: "mcp-ts-core 0.9.21 — per-request log context fix, secret-stripped error messages, fail-fast retries"
breaking: false
security: false
agent-notes: |
  Framework dep @cyanheads/mcp-ts-core ^0.9.16 → ^0.9.21.
  Notable changes picked up:
  - HTTP transport per-request log context fix: per-request logs and traces now carry fresh
    request + trace/span IDs instead of the frozen boot context.
  - fetchWithTimeout strips query-string secrets (e.g. ?api_key=) from error messages and logs.
  - withRetry fail-fast on non-retryable errors; ctx.fail auto-populates the retryable flag.
  No source changes required for this server.
---

# 0.1.9 — 2026-06-02

## Changed

- **`@cyanheads/mcp-ts-core` ^0.9.16 → ^0.9.21** — HTTP transport per-request log context fix (per-request logs and traces carry fresh request + trace/span IDs instead of the frozen boot context); `fetchWithTimeout` strips query-string secrets (e.g. `?api_key=`) from error messages and logs; `withRetry` fail-fast on non-retryable errors; `ctx.fail` auto-populates the `retryable` flag.
- **`release:github` script** — new `scripts/release-github.ts` and `package.json` `release:github` entry; new `scripts/check-skill-versions.ts` and `scripts/devcheck.ts` updates align with framework tooling.
- **README client config** — server key renamed from `nist-nvd` to `nist-nvd-mcp-server`; `args` updated to use the scoped package name `@cyanheads/nist-nvd-mcp-server@latest`.

## Dependencies

- `@cyanheads/mcp-ts-core` ^0.9.16 → ^0.9.21
- `vitest` ^4.1.7 → ^4.1.8
